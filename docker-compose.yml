services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: sgduf
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    volumes:
      - pgdata:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5

  backend:
    build: ./server
    ports:
      - "5000:5000"
    environment:
      DB_HOST: db
      DB_PORT: 5432
      DB_NAME: sgduf
      DB_USER: postgres
      DB_PASSWORD: postgres
      JWT_SECRET: sgduf_dev_secret_key_2024
      JWT_EXPIRES_IN: 24h
      PORT: 5000
      TAX_RATE: "0.12"
      NODE_ENV: development
    depends_on:
      db:
        condition: service_healthy

  frontend:
    build: ./client
    ports:
      - "3000:80"
    depends_on:
      - backend

volumes:
  pgdata:
